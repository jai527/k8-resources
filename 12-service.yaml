apiVersion: v1
kind: Service 
metadata:
  name: nginx

spec:
  selector:
    # labels are used as selectors
    project: roboshop
    environment: dev
    purpose: poc

ports:
- protocal: TCP 
  port: 80    # service port
  targetport: 80 #container port