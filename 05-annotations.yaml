apiVersion: v1
kind: Pod

metadata:
  name: annotations
  labels:
    project: roboshop
    environment: dev
    purpose: poc
  annotations:
    imageregistory: "http://hub.docker.com/"
    jenkinsURL: "https://jenkins.joindevops.com/build/job/roboshop/34" 
spec:
containers:
- name: nginx
  image: nginx