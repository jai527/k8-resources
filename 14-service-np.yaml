apiVersion: v1
kind: Service

metadata:
  name: nginx-np

spec:
  type: NodePort
  selector:
    project: roboshop
    environment: dev
    purpose: poc
  ports:
  - protocol: TCP
    port: 80  # service port
    targetPort: 80 # container port
    NodePort: 32149